-# -*- encoding: utf-8 -*-
!!!
= stylesheet_link_tag 'bootstrap.min', 'style'
%title かけいぼ！

.navbar.navbar-fixed-top
  .navbar-inner
    .container
      .brand かけいぼ！
      - form_tag '/', :method => 'post' do
        = select_tag :genre, :options => CONFIG.to_a.map{|genre| genre.reverse}, :class => 'span2'
        = text_field_tag :title, :value => params[:title], :placeholder => 'なにに', :required => ''
        = text_field_tag :price, :value => params[:price], :placeholder => 'いくら', :type => 'number', :required => '', :step => '100', :min => '100', :class => 'span2'
        = submit_tag 'つかった', :class => 'btn'

.container
  = yield
